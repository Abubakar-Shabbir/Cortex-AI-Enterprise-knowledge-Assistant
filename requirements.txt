# Core
Django==6.0.6
django-environ==0.14.0
python-dotenv==1.2.2
psycopg2-binary==2.9.12
pgvector==0.5.0

# Retrieval / NLP
sentence-transformers==5.6.1
rank-bm25==0.2.2
numpy==2.5.1
langchain-text-splitters==1.1.2
langchain-experimental==0.4.2
langchain-community==0.4.2

# Document parsing
pymupdf==1.28.0
python-docx==1.2.0

# LLM
google-genai==2.17.0
requests==2.34.2

# Profile module - avatar uploads (Django ImageField) and real
# device/browser/OS parsing for login history (RAG/services/
# device_intelligence_service.py) - deliberately not hand-rolled regex,
# see that module's docstring
Pillow==12.3.0
user-agents==2.2.0

# Answer rendering - Markdown -> sanitized HTML for Ask AI / AI Tasks
# LLM output (citation_service.render_answer_html())
markdown==3.10.3
nh3==0.3.6

# Background document processing and AI Tasks run on an in-process
# concurrent.futures.ThreadPoolExecutor (RAG/services/task_runner.py) -
# stdlib, no dependency needed. Celery/Redis were removed in the
# free-tier refactor.
